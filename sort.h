#include <cstdio>

void swap_int (int * left, int * right);

int sort_int_array (int * array, int start, int end, int pivot);

void print_int_array (int * array, int size);

bool find_sum (int * array, int size, int sum);
